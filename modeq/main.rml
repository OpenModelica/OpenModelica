(* main.rml - Modelica main program *)
module Main:
	relation main: string list => ()
end

with "class.rml"
with "absyn.rml"
with "parse.rml"
(* with "test.rml" *)
with "dump.rml"

(* with "modelica.rml" *)

relation main =
(*
		rule	Modelica.test1 &
		Modelica.test2 &
		Modelica.test3
		------------------------
		main _
*)

   rule print "Jahaja\n" &
	Parser.parse f => p &
	Dump.dump p
	(* & Dump.dump Test.prog1 *)
	& print "[slut]\n"
	----------------
	main f::_
end

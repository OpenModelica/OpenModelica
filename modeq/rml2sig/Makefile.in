#
# Makefile for rml2sig
#
# Andrzej BEDNARSKI
#
# $Id$

LEX=@LEX@
LEXLIB=@LEXLIB@

all: rml2sig

rml2sig: rml2sig.c
	gcc -o $@ $< $(LEXLIB)

rml2sig.c: rml2sig.lex
	$(LEX) -t rml2sig.lex > $@

clean:
	$(RM) rml2sig.c rml2sig

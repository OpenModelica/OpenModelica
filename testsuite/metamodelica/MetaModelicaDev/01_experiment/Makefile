# There is also an interactive solution, but until OpenModelica
# supports calling the same function twice, it will be disabled.
SOLUTIONS=SOLUTION_compiled.mos
CLEAN=Functions_* Main_* Types_*
include ../common.omc

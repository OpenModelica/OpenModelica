In this exercise you can experiment with MetaModelica:
\begin{itemize}
\item Types
\item Constants
\item Functions
\end{itemize}

Write some functions in \code{Functions.mo} to  display the complex constants defined in \code{Types.mo}.
Search for \code{// your code here} in \code{Main.mo} and \code{Functions.mo}.

\code{SOLUTION.txt} presents the solution to the assignment.

To run the calculator type \code{run} when building the project (or \code{make run} from a terminal).

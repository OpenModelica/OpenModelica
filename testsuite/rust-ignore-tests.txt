# Tests known to fail in the Rust port for reasons we accept (not codegen bugs).
# Used to filter failed.frontend-refactor-deps before triaging.
#
# The parser-position and Integer-width groups below are also marked in the test
# files themselves with '// suite: antlr' and '// suite: 63bit', so partest can
# deselect them with -suites=-antlr,-63bit. Keep the two in sync.
#
# Rust aborts the process on stack overflow (no catchable recursion limit),
# unlike MMC which unwinds out of its SEGV handler. A test that recurses without
# bound to reach that error message differs; it carries '// suite: stackoverflow'.
./flattening/modelica/algorithms-functions/StackOverflowTest.mos
#
# Parser error position: the hand-written recursive-descent parser reports a
# "No viable alternative near token: X" syntax error at a different token than
# ANTLR's error recovery picked. The error is still raised and the file still
# fails to parse; only which token/line:col is named differs.
./openmodelica/parser/Class4.mo
./openmodelica/parser/Declaration1.mo
./openmodelica/parser/Declaration2.mo
./openmodelica/parser/Declaration3.mo
./openmodelica/parser/Identifier.mo
./openmodelica/parser/ParseError2.mo
./openmodelica/parser/RealOpLexerModelica.mo
./openmodelica/parser/WildLexerMetaModelica.mo
# Error reporting wrong column/line
./simulation/modelica/others/TestSolve18.mos
./openmodelica/conversion/ConvertModifiersMissingValue1.mos
# PureImpure3 now parses correctly (pure/impure are demoted to identifiers under
# --std=<3.3 --strict, so `pure function` is rejected at 9:1-9:5 as it should
# be); only the wording differs ("No viable alternative near token: pure" vs
# ANTLR's "Parser error: Unexpected token near: pure (IDENT)").
./openmodelica/parser/PureImpure3.mo
#
# Lexer error message + position on invalid input. The hand-written lexer
# rejects the bad character (so the file still fails to lex, as expected) but
# reports e.g. "unexpected character: '#'" where ANTLR reports "Lexer failed to
# recognize '#'" / "Lexer got '& ' but failed to recognize the rest: '…'". The
# column also follows ANTLR's internal recovery point (after the partially
# consumed token), which our lexer does not reproduce.
./openmodelica/parser/ParseString.mos
./openmodelica/parser/MetaModelicaStringOpModelicaLexer.mo
#
# Integer width: MMC modelica_integer is 63-bit; the Rust port uses i32.
# Tests asserting the 63-bit max literal / wide-integer overflow behaviour differ.
# The min/max reductions over an empty array seed with +-intMaxLit()
# (=LONG_MAX/2 = 4611686018427387903), which does not fit in i32; the only diffs
# in these tests are those sentinel values.
./flattening/modelica/mosfiles/IntMulOverflow.mos
./openmodelica/parser/IntegerLiterals64.mo
./flattening/modelica/scodeinst/FuncBuiltinMax.mo
./flattening/modelica/scodeinst/FuncBuiltinMax2.mo
./flattening/modelica/scodeinst/FuncBuiltinMin.mo
./flattening/modelica/scodeinst/FuncBuiltinMin2.mo
./flattening/modelica/scodeinst/FuncBuiltinReduction.mo
./flattening/modelica/scodeinst/CevalReduction1.mo
# IntegerTest passes with the Rust port on the C runtime (modelica_integer is
# 64-bit there). It only differs under --simCodeTarget=wasm-jit, whose runtime
# Integer is i32, so `2147483647 * 2` wraps to -2 instead of 4294967294. Not an
# ignore entry — it carries '// suite: 63bit', which the wasm-jit run deselects
# and the C-runtime run keeps.
#./simulation/modelica/types/IntegerTest.mos
# saveTotalModel(obfuscate=true) replaces assert/terminate messages with
# "<fn> message <stringHashDjb2(msg)>"; C's stringHashDjb2 accumulates djb2 in a
# 64-bit modelica_integer (then labs), the port's Integer is i32 so the embedded
# hash value differs. Only those hash numbers differ; not fixable without i64.
./openmodelica/interactive-API/Obfuscation1.mos
# Backend does operations in different order
./simulation/modelica/NBackend/differentation/adjoint_jacobian2.mos
./simulation/modelica/NBackend/functions/function_annotation_der.mos
./simulation/modelica/NBackend/index_reduction/static_IR.mos
./simulation/modelica/NBackend/initialization/TestInversePlant.mos
./simulation/modelica/NBackend/simplification/splitIf.mos
./simulation/modelica/start_value_selection/asmaFlow.mos
# Fails also for bootstrapped OMC with the same limits
./simulation/modelica/NBackend/ScalableTestsuite/BreakerNetwork.mos
# Star bicoloring (bidirectional Jacobian) calls the external "C"
# ColPackBicoloring_starBicolor, a C++ ColPack wrapper not yet ported/FFI'd in
# the Rust omc; colpackStarBicoloring hits its todo!() placeholder.
./simulation/modelica/NBackend/bicoloring/arrowhead.mos

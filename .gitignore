/_CPack_Packages/
/.cache/
/.claude/
/.externalToolBuilders/*.launch
/.mailmap
/.metadata/
/.project
/.qtcreator/
/.settings
/.vscode/
/build_cmake*/
/build-testsuite-ctest/
/build/
/install_cmake/
/install/
/issues/
/OMCompiler/SimulationRuntime/ModelicaExternalC/build/
/OMEncryption/
/OMJava/
/OMPublicKey/
/OMPython/
/OMSetup/
/OpenModelicaSetup/
/testsuite/flattening/modelica/ffi/FFITest/Resources/BuildProjects/gcc/libFFITestLib.dll
/testsuite/libraries-for-testing/.ccache/
trace.txt

# gcov's per-source annotations. gcovr writes these in whatever directory it
# runs gcov from and normally deletes them again, but a failed run - or gcov
# invoked by hand - leaves them behind, including in the source tree.
*.gcov

# Claude handoffs
**/HANDOFF*.md

# Cloud storage OAuth client registrations. Not ignored because the contents are
# secret - a browser client serves them to every visitor - but because the consent
# screen carries the registering organisation's name, so another party's app built
# on a committed client would appear to users as OpenModelica, and could get the
# client suspended for everyone. Ship the .example; supply the real file at deploy
# time. (GOCSPX- values also trip GitHub secret scanning.)
cloud_config.json

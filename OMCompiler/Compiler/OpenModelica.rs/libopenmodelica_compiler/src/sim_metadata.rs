// @generated by libopenmodelica_compiler/gen/gen-sim-tables.sh — do not edit.
//
// Simulation/flag/log-stream metadata tables OMEdit reads, mirrored from the
// OpenModelica C runtime (SimulationRuntime) so OMEdit can link the Rust
// libOpenModelicaCompiler.so without the C runtime / Boehm GC. Regenerate with
// that script when the upstream tables change.
#![allow(non_upper_case_globals)]
use core::ffi::c_char;

// `const char* NAME[N]` in C: an array of pointers to NUL-terminated strings.
// Wrapped so the `static` is `Sync` (raw pointers are not); `repr(transparent)`
// keeps the symbol's layout identical to the C array.
#[repr(transparent)]
pub struct CStrTable<const N: usize>(pub [*const c_char; N]);
// SAFETY: the pointers reference immutable, 'static c-string literals.
unsafe impl<const N: usize> Sync for CStrTable<N> {}

#[unsafe(no_mangle)]
pub static FLAG_NAME: CStrTable<156> = CStrTable([
    c"FLAG_UNKNOWN".as_ptr(),
    c"abortSlowSimulation".as_ptr(),
    c"alarm".as_ptr(),
    c"clock".as_ptr(),
    c"cpu".as_ptr(),
    c"csvOstep".as_ptr(),
    c"cvodeNonlinearSolverIteration".as_ptr(),
    c"cvodeLinearMultistepMethod".as_ptr(),
    c"cx".as_ptr(),
    c"daeMode".as_ptr(),
    c"deltaXLinearize".as_ptr(),
    c"deltaXSolver".as_ptr(),
    c"embeddedServer".as_ptr(),
    c"embeddedServerPort".as_ptr(),
    c"mat_sync".as_ptr(),
    c"emit_protected".as_ptr(),
    c"eps".as_ptr(),
    c"f".as_ptr(),
    c"help".as_ptr(),
    c"homAdaptBend".as_ptr(),
    c"homBacktraceStrategy".as_ptr(),
    c"homHEps".as_ptr(),
    c"homMaxLambdaSteps".as_ptr(),
    c"homMaxNewtonSteps".as_ptr(),
    c"homMaxTries".as_ptr(),
    c"homNegStartDir".as_ptr(),
    c"homotopyOnFirstTry".as_ptr(),
    c"noHomotopyOnFirstTry".as_ptr(),
    c"homTauDecFac".as_ptr(),
    c"homTauDecFacPredictor".as_ptr(),
    c"homTauIncFac".as_ptr(),
    c"homTauIncThreshold".as_ptr(),
    c"homTauMax".as_ptr(),
    c"homTauMin".as_ptr(),
    c"homTauStart".as_ptr(),
    c"idaMaxErrorTestFails".as_ptr(),
    c"idaMaxNonLinIters".as_ptr(),
    c"idaMaxConvFails".as_ptr(),
    c"idaNonLinConvCoef".as_ptr(),
    c"idaLS".as_ptr(),
    c"idaScaling".as_ptr(),
    c"idaSensitivity".as_ptr(),
    c"ignoreHideResult".as_ptr(),
    c"iif".as_ptr(),
    c"iim".as_ptr(),
    c"iit".as_ptr(),
    c"ils".as_ptr(),
    c"initialStepSize".as_ptr(),
    c"csvInput".as_ptr(),
    c"stateFile".as_ptr(),
    c"inputPath".as_ptr(),
    c"ipopt_hesse".as_ptr(),
    c"ipopt_init".as_ptr(),
    c"ipopt_jac".as_ptr(),
    c"ipopt_max_iter".as_ptr(),
    c"ipopt_warm_start".as_ptr(),
    c"jacobian".as_ptr(),
    c"jacobianThreads".as_ptr(),
    c"l".as_ptr(),
    c"l_datarec".as_ptr(),
    c"logFormat".as_ptr(),
    c"ls".as_ptr(),
    c"ls_ipopt".as_ptr(),
    c"lss".as_ptr(),
    c"lssMaxDensity".as_ptr(),
    c"lssMinSize".as_ptr(),
    c"lv".as_ptr(),
    c"lvMaxWarn".as_ptr(),
    c"lv_time".as_ptr(),
    c"lv_system".as_ptr(),
    c"mbi".as_ptr(),
    c"mei".as_ptr(),
    c"maxIntegrationOrder".as_ptr(),
    c"maxStepSize".as_ptr(),
    c"measureTimePlotFormat".as_ptr(),
    c"moo".as_ptr(),
    c"moo_l2bn_p1_it".as_ptr(),
    c"moo_l2bn_p2_it".as_ptr(),
    c"moo_l2bn_p2_lvl".as_ptr(),
    c"newtonFTol".as_ptr(),
    c"newtonMaxSteps".as_ptr(),
    c"newtonMaxStepFactor".as_ptr(),
    c"newtonXTol".as_ptr(),
    c"newtonJacUpdates".as_ptr(),
    c"newton".as_ptr(),
    c"nls".as_ptr(),
    c"nlsInfo".as_ptr(),
    c"nlsLS".as_ptr(),
    c"nlssMaxDensity".as_ptr(),
    c"nlssMinSize".as_ptr(),
    c"nlsJacTestATol".as_ptr(),
    c"nlsJacTestRTol".as_ptr(),
    c"noemit".as_ptr(),
    c"noEquidistantTimeGrid".as_ptr(),
    c"noEquidistantOutputFrequency".as_ptr(),
    c"noEquidistantOutputTime".as_ptr(),
    c"noEventEmit".as_ptr(),
    c"noRestart".as_ptr(),
    c"noRootFinding".as_ptr(),
    c"noScaling".as_ptr(),
    c"noSuppressAlg".as_ptr(),
    c"optDebugJac".as_ptr(),
    c"optimizerNP".as_ptr(),
    c"optimizerTimeGrid".as_ptr(),
    c"output".as_ptr(),
    c"outputFormat".as_ptr(),
    c"outputPath".as_ptr(),
    c"override".as_ptr(),
    c"overrideFile".as_ptr(),
    c"port".as_ptr(),
    c"r".as_ptr(),
    c"reconcile".as_ptr(),
    c"reconcileBoundaryConditions".as_ptr(),
    c"reconcileState".as_ptr(),
    c"gbm".as_ptr(),
    c"gbctrl".as_ptr(),
    c"gbctrl_evnt_reinit".as_ptr(),
    c"gbctrl_filter".as_ptr(),
    c"gbctrl_fhr".as_ptr(),
    c"gberr".as_ptr(),
    c"gbint".as_ptr(),
    c"gbnls".as_ptr(),
    c"gbnls_internal_damping".as_ptr(),
    c"gbnls_internal_jackeep".as_ptr(),
    c"gbfm".as_ptr(),
    c"gbfctrl".as_ptr(),
    c"gbferr".as_ptr(),
    c"gbfint".as_ptr(),
    c"gbfnls".as_ptr(),
    c"gbratio".as_ptr(),
    c"rt".as_ptr(),
    c"s".as_ptr(),
    c"saveInitialGuess_system".as_ptr(),
    c"single".as_ptr(),
    c"steps".as_ptr(),
    c"startTime".as_ptr(),
    c"steadyState".as_ptr(),
    c"steadyStateTol".as_ptr(),
    c"stepSize".as_ptr(),
    c"stopAtSystem".as_ptr(),
    c"stopTime".as_ptr(),
    c"svdCount".as_ptr(),
    c"svdSigma".as_ptr(),
    c"sx".as_ptr(),
    c"tolerance".as_ptr(),
    c"keepHessian".as_ptr(),
    c"variableFilter".as_ptr(),
    c"w".as_ptr(),
    c"parmodNumThreads".as_ptr(),
    c"parmodScheduler".as_ptr(),
    c"parmodClustering".as_ptr(),
    c"parmodClustersPerLevel".as_ptr(),
    c"parmodDumpTaskGraph".as_ptr(),
    c"parmodImportClustering".as_ptr(),
    c"parmodDumpStages".as_ptr(),
    c"FLAG_MAX".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static FLAG_DETAILED_DESC: CStrTable<156> = CStrTable([
    c"unknown".as_ptr(),
    c"  Aborts if the simulation chatters.".as_ptr(),
    c"  Aborts after the given number of seconds (default=0 disables the alarm).".as_ptr(),
    c"  Selects the type of clock to use. Valid options include:\x0a\x0a  * RT (monotonic real-time clock)\x0a  * CYC (cpu cycles measured with RDTSC)\x0a  * CPU (process-based CPU-time)".as_ptr(),
    c"  Dumps the cpu-time into the result file using the variable named $cpuTime.".as_ptr(),
    c"  Value specifies csv-files for debug values for optimizer step.".as_ptr(),
    c"  Nonlinear solver iteration for CVODE solver. Default: Depends on flag cvodeLinearMultistepMethod. Valid values\x0a\x0a  * CV_ITER_NEWTON       - Newton iteration.\x0a                           Advised to use together with flag -cvodeLinearMultistepMethod=CV_BDF.\x0a  * CV_ITER_FIXED_POINT  - Fixed-Point iteration iteration.\x0a                           Advised to use together with flag -cvodeLinearMultistepMethod=CV_ADAMS.".as_ptr(),
    c"  Linear multistep method for CVODE solver. Default: CV_BDF. Valid values\x0a\x0a  * CV_BDF    - BDF linear multistep method for stiff problems.\x0a                Use together with flag -cvodeNonlinearSolverIteration=CV_ITER_NEWTON or don't set cvodeNonlinearSolverIteration.\x0a  * CV_ADAMS  - Adams-Moulton linear multistep method for nonstiff problems.\x0a                Use together with flag -cvodeNonlinearSolverIteration=CV_ITER_FIXED_POINT or don't set cvodeNonlinearSolverIteration.".as_ptr(),
    c"  Value specifies an csv-file with inputs as correlation coefficient matrix Cx for DataReconciliation".as_ptr(),
    c"  Enables daeMode simulation if the model was compiled with the omc flag --daeMode and ida method is used.".as_ptr(),
    c"  Value specifies the delta x value for numerical differentiation used by linearization. The default value is sqrt(DBL_EPSILON*2e1).".as_ptr(),
    c"  Value specifies the delta x value for numerical differentiation used by integration method. The default values is sqrt(DBL_EPSILON).".as_ptr(),
    c"  Enables an embedded server. Valid values:\x0a\x0a  * none - default, run without embedded server\x0a  * opc-da - [broken] run with embedded OPC DA server (WIN32 only, uses proprietary OPC SC interface)\x0a  * opc-ua - [experimental] run with embedded OPC UA server (TCP port 4841 for now; will have its own configuration option later)\x0a  * filename - path to a shared object implementing the embedded server interface (requires access to internal OMC data-structures if you want to read or write data)".as_ptr(),
    c"  Value specifies the port number used by the embedded server. The default value is 4841.".as_ptr(),
    c"  Syncs the mat file header after emitting every N time-points.".as_ptr(),
    c"  Emits protected variables to the result-file.".as_ptr(),
    c"  Value specifies the number of convergence iteration to be performed for DataReconciliation".as_ptr(),
    c"  Value specifies a new setup XML file to the generated simulation code.\x0a".as_ptr(),
    c"  Get detailed information that specifies the command-line flag\x0a\x0a  For example, -help=f prints detailed information for command-line flag f.".as_ptr(),
    c"  Maximum trajectory bending to accept the homotopy step.\x0a  Default: 0.5, which means the corrector vector has to be smaller than half of the predictor vector.".as_ptr(),
    c"  Value specifies the backtrace strategy in the homotopy corrector step. Valid values:\x0a\x0a  * fix - default, go back to the path by fixing one coordinate\x0a  * orthogonal - go back to the path in an orthogonal direction to the tangent vector".as_ptr(),
    c"  Tolerance respecting residuals for the homotopy H-function (default: 1e-5).\x0a\x0a  In the last step (lambda=1) newtonFTol is used as tolerance.".as_ptr(),
    c"  Maximum lambda steps allowed to run the homotopy path (default: system size * 100).".as_ptr(),
    c"  Maximum newton steps in the homotopy corrector step (default: 20).".as_ptr(),
    c"  Maximum number of tries for one homotopy lambda step (default: 10).".as_ptr(),
    c"  Start to run along the homotopy path in the negative direction.\x0a\x0a  If one direction fails, the other direction is always used as fallback option.".as_ptr(),
    c"  If the model contains the homotopy operator, directly use the homotopy method to solve the initialization problem.\x0a  This is already the default behaviour, this flag can be used to undo the effect of -noHomotopyOnFirstTry".as_ptr(),
    c"  Disable the use of the homotopy method to solve the initialization problem.\x0a  Without this flag, the solver tries to solve the initialization problem with homotopy if the model contains the homotopy-operator.".as_ptr(),
    c"  Decrease homotopy step size tau by this factor if tau is too big in the homotopy corrector step (default: 10.0).".as_ptr(),
    c"  Decrease homotopy step size tau by this factor if tau is too big in the homotopy predictor step (default: 2.0).".as_ptr(),
    c"  Increase homotopy step size tau by this factor if tau can be increased after the homotopy corrector step (default: 2.0).".as_ptr(),
    c"  Increase the homotopy step size tau if homAdaptBend/bend > homTauIncThreshold (default: 10).".as_ptr(),
    c"  Maximum homotopy step size tau for the homotopy process (default: 10).".as_ptr(),
    c"  Minimum homotopy step size tau for the homotopy process (default: 1e-4).".as_ptr(),
    c"  Homotopy step size tau at the beginning of the homotopy process (default: 0.2).".as_ptr(),
    c"  Value specifies the maximum number of error test failures in attempting one step. The default value is 7.".as_ptr(),
    c"  Value specifies the maximum number of nonlinear solver iterations at one step. The default value is 3.".as_ptr(),
    c"  Value specifies the maximum number of nonlinear solver convergence failures at one step. The default value is 10.".as_ptr(),
    c"  Value specifies the safety factor in the nonlinear convergence test. The default value is 0.33.".as_ptr(),
    c"  Value specifies the linear solver of the ida integration method. Valid values:\x0a".as_ptr(),
    c"  Enable scaling of the IDA solver.".as_ptr(),
    c"  Enables sensitivity analysis with respect to parameters if the model is compiled with omc flag --calculateSensitivities.".as_ptr(),
    c"  Emits also variables with HideResult=true annotation.".as_ptr(),
    c"  Value specifies an external file for the initialization of the model.".as_ptr(),
    c"  Value specifies the initialization method.\x0a  Following options are available: 'symbolic' (default) and 'none'.".as_ptr(),
    c"  Value [Real] specifies a time for the initialization of the model.".as_ptr(),
    c"  Value specifies the number of steps for homotopy method (required: -iim=symbolic).\x0a  The value is an Integer with default value 3.".as_ptr(),
    c"  Value specifies an initial step size, used by the methods: dassl, ida, gbode".as_ptr(),
    c"  Value specifies an csv-file with inputs for the simulation/optimization of the model".as_ptr(),
    c"  Value specifies an file with states start values for the optimization of the model.".as_ptr(),
    c"  Value specifies a path for reading the input files i.e., model_init.xml and model_info.json".as_ptr(),
    c"  Value specifies the hessematrix for Ipopt(OMC, BFGS, const).".as_ptr(),
    c"  Value specifies the initial guess for optimization (sim, const).".as_ptr(),
    c"  Value specifies the Jacobian for Ipopt(SYM, NUM, NUMDENSE).".as_ptr(),
    c"  Value specifies the max number of iteration for ipopt.".as_ptr(),
    c"  Value specifies lvl for a warm start in ipopt: 1,2,3,...".as_ptr(),
    c"  Select the calculation method for Jacobian used by the integration method:\x0a".as_ptr(),
    c"  Value specifies the number of threads for jacobian evaluation in dassl or ida.  The value is an Integer with default value 1.".as_ptr(),
    c"  Value specifies a time where the linearization of the model should be performed.".as_ptr(),
    c"  Emit data recovery matrices with model linearization.".as_ptr(),
    c"  Value specifies the log format of the executable:\x0a\x0a  * text (default)\x0a  * xml\x0a  * xmltcp (required -port flag)".as_ptr(),
    c"  Value specifies the linear solver method".as_ptr(),
    c"  Value specifies the linear solver method for Ipopt, default mumps.\x0a  Note: Use if you build ipopt with other linear solver like ma27".as_ptr(),
    c"  Value specifies the linear sparse solver method".as_ptr(),
    c"  Value specifies the maximum density for using a linear sparse solver.\x0a  The value is a Double with default value 0.2.".as_ptr(),
    c"  Value specifies the minimum system size for using a linear sparse solver.\x0a  The value is an Integer with default value 1000.".as_ptr(),
    c"  Value (a comma-separated String list) specifies which logging levels to\x0a  enable. Multiple options can be enabled at the same time.".as_ptr(),
    c"  Maximum number of times some repeating warnings are displayed.\x0a  Default value 3.".as_ptr(),
    c"  Interval (a comma-separated Double list with two elements) specifies in which\x0a  time interval logging is active. Doesn't affect LOG_STDOUT, LOG_ASSERT, and\x0a  LOG_SUCCESS, LOG_STATS, LOG_STATS_V.".as_ptr(),
    c"  Value is a comma-separated list of equation indices (available in the transformational debugger) for which solver logs are shown (by default logs for all systems are shown)".as_ptr(),
    c"  Value specifies the maximum number of bisection iterations for state event\x0a  detection or zero for default behavior".as_ptr(),
    c"  Value specifies the maximum number of event iterations.\x0a  The value is an Integer with default value 20.".as_ptr(),
    c"  Value specifies maximum integration order, used by the methods: dassl, ida.".as_ptr(),
    c"  Value specifies maximum absolute step size, used by the methods: dassl, ida, gbode.".as_ptr(),
    c"  Value specifies the output format of the measure time functionality:\x0a\x0a  * svg\x0a  * jpg\x0a  * ps\x0a  * gif\x0a  * ...".as_ptr(),
    c"  Perform dynamic optimization with MOO library".as_ptr(),
    c"  Value specifies the number of phase I iterations (full bisections) for L2-Boundary-Norm mesh refinement in MOO".as_ptr(),
    c"  Value specifies the number of phase II iterations (refinement) for L2-Boundary-Norm mesh refinement in MOO".as_ptr(),
    c"  Value specifies the phase II refinement aggressiveness for L2-Boundary-Norm mesh refinement in MOO".as_ptr(),
    c"  Tolerance respecting residuals for updating solution vector in Newton solver.\x0a  Solution is accepted if the (scaled) 2-norm of the residuals is smaller than the tolerance newtonFTol and the (scaled) newton correction (delta_x) is smaller than the tolerance newtonXTol.\x0a  The value is a Double with default value 1e-12.".as_ptr(),
    c"  Maximum number of newton steps, used currently only by KINSOL in solver GBODE.".as_ptr(),
    c"  Maximum newton step factor mxnewtstep = maxStepFactor * norm2(xScaling).  Used currently only by KINSOL.".as_ptr(),
    c"  Tolerance respecting newton correction (delta_x) for updating solution vector in Newton solver.\x0a  Solution is accepted if the (scaled) 2-norm of the residuals is smaller than the tolerance newtonFTol and the (scaled) newton correction (delta_x) is smaller than the tolerance newtonXTol.\x0a  The value is a Double with default value 1e-12.".as_ptr(),
    c"  Number of steps before Jacobian is recomputed. If zero is chosen, the corresponding solution phase is skipped.".as_ptr(),
    c"  Value specifies the damping strategy for the newton solver.".as_ptr(),
    c"  Value specifies the nonlinear solver:".as_ptr(),
    c"  Outputs detailed information about solving process of non-linear systems into csv files.".as_ptr(),
    c"  Value specifies the linear solver used by the non-linear solver:".as_ptr(),
    c"  Value specifies the maximum density for using a non-linear sparse solver.\x0a  The value is a Double with default value 0.1.".as_ptr(),
    c"  Value specifies the minimum system size for using a non-linear sparse solver.\x0a  The value is an Integer with default value 1000.".as_ptr(),
    c"  Value specifies the absolute tolerance for the Jacobian derivative test.".as_ptr(),
    c"  Value specifies the relative tolerance for the Jacobian derivative test.".as_ptr(),
    c"  Do not emit any results to the result file.".as_ptr(),
    c"  Output the internal steps given by dassl/ida instead of interpolating results\x0a  into an equidistant time grid as given by stepSize or numberOfIntervals.".as_ptr(),
    c"  Integer value n controls the output frequency in noEquidistantTimeGrid mode\x0a  and outputs every n-th time step".as_ptr(),
    c"  Real value timeValue controls the output time point in noEquidistantOutputTime\x0a  mode and outputs every time>=k*timeValue, where k is an integer".as_ptr(),
    c"  Do not emit event points to the result file.".as_ptr(),
    c"  Disables the restart of the integration method after an event is performed, used by the methods: dassl, ida".as_ptr(),
    c"  Disables the internal root finding procedure of methods: dassl and ida.".as_ptr(),
    c"  Disables scaling for the variables and the residuals in the algebraic nonlinear solver KINSOL.".as_ptr(),
    c"  Flag to not suppress algebraic variables in the local error test of the ida solver in daeMode.\x0a  In general, the use of this option is discouraged when solving DAE systems of index 1,\x0a  whereas it is generally encouraged for systems of index 2 or more.".as_ptr(),
    c"  Value specifies the number of iterations from the dynamic optimization, which\x0a  will be debugged, creating .csv and .py files.".as_ptr(),
    c"  Value specifies the number of points in a subinterval.\x0a  Currently supports numbers 1 and 3.".as_ptr(),
    c"  Value specifies external file with time points.".as_ptr(),
    c"  Output the variables a, b and c at the end of the simulation to the standard\x0a  output: time = value, a = value, b = value, c = value".as_ptr(),
    c"  Sets file format of the result file.".as_ptr(),
    c"  Value specifies a path for writing the output files i.e., model_res.mat, model_prof.intdata, model_prof.realdata etc.".as_ptr(),
    c"  Override the variables in the XML setup file\x0a  For example: var1=start1,var2=start2,par3=start3".as_ptr(),
    c"  Will override the variables in the XML setup file\x0a  with the values from the file.\x0a  Note that: -overrideFile CANNOT be used with -override.\x0a  Use when variables for -override are too many.\x0a  overrideFileName contains lines of the form: var=start".as_ptr(),
    c"  Value specifies the port for simulation status (default disabled).".as_ptr(),
    c"  Value specifies the name of the output result file.\x0a  The default file-name is based on the model name and output format.\x0a  For example: Model_res.mat.".as_ptr(),
    c"  Run the Data Reconciliation numerical computation algorithm for constrained equations".as_ptr(),
    c"  Run the Data Reconciliation numerical computation algorithm for boundary condition equations".as_ptr(),
    c"  Run the State Estimation numerical computation algorithm for constrained equations".as_ptr(),
    c"  Value specifies the chosen solver of solver gbode (single-rate, slow states integrator).".as_ptr(),
    c"  Step size control of solver gbode (single-rate, slow states integrator).".as_ptr(),
    c"  Reset step size using standard inital step size selection after an event (default false)".as_ptr(),
    c"  Applies exponential smoothing to the step size factor; gbctrl_filter = 0 yields constant step size, gbctrl_filter = 1 uses full adaptation without averaging.".as_ptr(),
    c"  Applies adaptive damping to the step size factor using F\xc3\xbchrer\xe2\x80\x99s approach, scaling it by h_fac *= (h_n / h_n1)^gamma to penalize repeated rejections or reward successful step acceptance.".as_ptr(),
    c"  Error estimation method for solver gbode (single-rate, slow states integrator)\x0a  Possible values:\x0a\x0a    * default    - depending on the Runge-Kutta method\x0a    * richardson - Richardson extrapolation\x0a    * embedded   - Embedded scheme\x0a".as_ptr(),
    c"  Interpolation method of solver gbode (single-rate, slow states integrator).".as_ptr(),
    c"  Non-linear solver method of solver gbode (single-rate, slow states integrator).".as_ptr(),
    c"  Value specifies damping applied to the estimated convergence rate in the first Newton iteration (0 <= value <= 1; 0 = conservative, 1 = optimistic). Only valid for -gbnls=internal.\x0a  Since no history is available in the first Newton iteration, the convergence rate is taken from the previous solve and raised to the power of this value.".as_ptr(),
    c"  Value specifies how often the ODE Jacobian is recalculated (0 <= value < 1). Only valid for -gbnls=internal.\x0a  The Jacobian is kept, if the linear convergence rate || dz_k || / || dz_{k-1} || of the Newton iteration is smaller than the specified value. Small values result in more Jacobian callbacks.".as_ptr(),
    c"  Value specifies the chosen solver of solver gbode (multi-rate, fast states integrator).\x0a  Current Restriction: Fully implicit (Gauss, Radau, Lobatto) RK methods are not supported, yet.".as_ptr(),
    c"  Step size control of solver gbode (multi-rate, fast states integrator).".as_ptr(),
    c"  Error estimation method for solver gbode (multi-rate, fast states integrator)\x0a  Possible values:\x0a\x0a    * default    - depending on the Runge-Kutta method\x0a    * richardson - Richardson extrapolation\x0a    * embedded   - Embedded scheme\x0a".as_ptr(),
    c"  Interpolation method of solver gbode (multi-rate, fast states integrator).".as_ptr(),
    c"  Non-linear solver method of solver gbode (multi-rate, fast states integrator).".as_ptr(),
    c"  Define percentage of states for the fast states selection of solver gbode (values from 0 to 1).".as_ptr(),
    c"  Value specifies the scaling factor for real-time synchronization (0 disables).\x0a  A value > 1 means the simulation takes a longer time to simulate.\x0a".as_ptr(),
    c"  Value specifies the integration method. For additional information see the :ref:`User's Guide <cruntime-integration-methods>`".as_ptr(),
    c"  Debug flag that performs standard initialization until the specified system is reached, computes only the torn part and saves the results obtained so far to a .mat file".as_ptr(),
    c"  Output results in single precision (mat-format only).".as_ptr(),
    c"  Dumps the number of integration steps into the result file.".as_ptr(),
    c"  Sets startTime for the simulation.".as_ptr(),
    c"  Aborts the simulation if steady state is reached.".as_ptr(),
    c"  This relative tolerance is used to detect steady state: max(|d(x_i)/dt|/nominal(x_i)) < steadyStateTol".as_ptr(),
    c"  Sets stepSize for the simulation.".as_ptr(),
    c"  Performs standard initialization until the specified system is reached, then aborts the simulation.".as_ptr(),
    c"  Sets stopTime for the simulation.".as_ptr(),
    c"  Number of extremal singular values and vectors computed for LOG_NLS_SVD (0 disables).".as_ptr(),
    c"  Estimated smallest singular value for the preconditioner in SVD analysis.".as_ptr(),
    c"  Value specifies an csv-file with inputs as covariance matrix Sx for DataReconciliation".as_ptr(),
    c"  Specifies solver tolerance.".as_ptr(),
    c"  Value specifies the number of steps, which keep Hessian matrix constant.".as_ptr(),
    c"  Specifies which variables should be present in the result-file using POSIX Extended Regular Expressions. The given expression must match the full variable name.".as_ptr(),
    c"  Shows all warnings even if a related log-stream is inactive.".as_ptr(),
    c"  Value specifies the number of threads for simulation using parmodauto. If not specified (or is 0) it will use the systems max number of threads. Note that this option is ignored if the model is not compiled with --parmodauto".as_ptr(),
    c"  Selects the parmodauto scheduler: 'flow' (default) or 'level'. Switchable at run time without recompiling the simulator.".as_ptr(),
    c"  Selects the parmodauto clustering strategy: 'default' (merge-based), 'fixed_width_min_height', or 'none'.".as_ptr(),
    c"  Sets the maximum number of clusters per level used by the default clustering.".as_ptr(),
    c"  Exports the parmodauto task graph (tasks, dependencies) and the resulting clustering to the given json file, keyed by equation index.".as_ptr(),
    c"  Imports a clustering (groups of equation indices) from the given json file instead of computing one. The simulation aborts if the clustering is invalid (forms a cycle or references unknown equations).".as_ptr(),
    c"  Exports the parmodauto task graph and clustering before and after each clustering optimization to a series of json files named <prefix>.NN.<stage>.json, so the effect of each optimization can be inspected.".as_ptr(),
    c"FLAG_MAX".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static SOLVER_METHOD_NAME: CStrTable<11> = CStrTable([
    c"unknown".as_ptr(),
    c"dassl".as_ptr(),
    c"ida".as_ptr(),
    c"cvode".as_ptr(),
    c"gbode".as_ptr(),
    c"euler".as_ptr(),
    c"rungekutta".as_ptr(),
    c"symSolver".as_ptr(),
    c"symSolverSsc".as_ptr(),
    c"qss".as_ptr(),
    c"optimization".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static SOLVER_METHOD_DESC: CStrTable<11> = CStrTable([
    c"unknown".as_ptr(),
    c"dassl (default) - BDF method - implicit (dense solver), variable step size control, adaptive order 1-5, event location".as_ptr(),
    c"ida - SUNDIALS IDA solver - BDF method - implicit (sparse/dense solver, default sparse) variable step size control, adaptive order 1-5, event location - additional simulation flags: -idaMaxErrorTestFails -idaMaxNonLinIters -idaMaxConvFails -idaNonLinConvCoef -idaLS -idaScaling -idaSensitivity".as_ptr(),
    c"cvode - SUNDIALS CVODE solver - BDF or Adams-Moulton solver - implicit (dense solver), variable step-size control, adaptive order 1-12, event location - additional simulation flags -cvodeLinearMultistepMethod -cvodeNonlinearSolverIteration".as_ptr(),
    c"gbode - generic Runge-Kutta ODE solver - implicit (sparse solver)/explicit, fixed/variable step size control, order 1-14, event location, optional bi-rate integration - additional simulation flags -gbm -gbctrl -gbratio - additional advanced flags -gbctrl_filter -gbctrl_fhr -gberr -gbint -gbnls -gbfm -gbfctrl -gbferr -gbfint -gbfnls".as_ptr(),
    c"euler - explicit Euler, fixed step size, order 1".as_ptr(),
    c"rungekutta - classical Runge-Kutta - explicit, fixed step, order 4".as_ptr(),
    c"symSolver - symbolic inline Solver [compiler flag '--symSolver' needed] - fixed step size, order 1".as_ptr(),
    c"symSolverSsc - symbolic implicit Euler with step size control [compiler flag '--symSolver' needed] - step size control, order 1".as_ptr(),
    c"qss - A QSS solver [experimental]".as_ptr(),
    c"optimization - Special solver for dynamic optimization".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static INIT_METHOD_NAME: CStrTable<3> = CStrTable([
    c"unknown".as_ptr(),
    c"none".as_ptr(),
    c"symbolic".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static INIT_METHOD_DESC: CStrTable<3> = CStrTable([
    c"unknown".as_ptr(),
    c"sets all variables to their start values and skips the initialization process".as_ptr(),
    c"solves the initialization problem symbolically - default".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static JACOBIAN_METHOD_NAME: CStrTable<7> = CStrTable([
    c"unknown".as_ptr(),
    c"coloredNumerical".as_ptr(),
    c"internalNumerical".as_ptr(),
    c"coloredSymbolical".as_ptr(),
    c"coloredSymbolicalAdjoint".as_ptr(),
    c"numerical".as_ptr(),
    c"symbolical".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static JACOBIAN_METHOD_DESC: CStrTable<7> = CStrTable([
    c"unknown".as_ptr(),
    c"Colored numerical Jacobian, which is default for dassl and ida. Needs omc compiler flag --generateDynamicJacobian=numeric. With option -idaLS=klu a sparse matrix is used.".as_ptr(),
    c"Dense solver internal numerical Jacobian.".as_ptr(),
    c"Colored symbolical Jacobian. Needs omc compiler flag --generateDynamicJacobian=symbolic. With option -idaLS=klu a sparse matrix is used.".as_ptr(),
    c"Colored symbolical adjoint Jacobian. Needs omc compiler flags --newBackend and --generateDynamicJacobian=symbolicadjoint.".as_ptr(),
    c"Dense numerical Jacobian.".as_ptr(),
    c"Dense symbolical Jacobian. Needs omc compiler flag --generateDynamicJacobian=symbolic.".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static LS_NAME: CStrTable<7> = CStrTable([
    c"LS_UNKNOWN".as_ptr(),
    c"lapack".as_ptr(),
    c"lis".as_ptr(),
    c"klu".as_ptr(),
    c"umfpack".as_ptr(),
    c"totalpivot".as_ptr(),
    c"default".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static LS_DESC: CStrTable<7> = CStrTable([
    c"unknown".as_ptr(),
    c"method using LAPACK LU factorization".as_ptr(),
    c"method using iterative solver Lis".as_ptr(),
    c"method using KLU sparse linear solver".as_ptr(),
    c"method using UMFPACK sparse linear solver".as_ptr(),
    c"method using a total pivoting LU factorization for underdetermination systems".as_ptr(),
    c"default method - LAPACK with total pivoting as fallback".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static NLS_NAME: CStrTable<7> = CStrTable([
    c"NLS_UNKNOWN".as_ptr(),
    c"hybrid".as_ptr(),
    c"kinsol".as_ptr(),
    c"experimental-kinsol".as_ptr(),
    c"newton".as_ptr(),
    c"mixed".as_ptr(),
    c"homotopy".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static NLS_DESC: CStrTable<7> = CStrTable([
    c"unknown".as_ptr(),
    c"Modification of the Powell hybrid method from minpack - former default solver".as_ptr(),
    c"SUNDIALS/KINSOL includes an interface to the sparse direct solver, KLU. See simulation option -nlsLS for more information.".as_ptr(),
    c"SUNDIALS/KINSOL (with internal scaling) includes an interface to the sparse direct solver, KLU. See simulation option -nlsLS for more information.".as_ptr(),
    c"Newton Raphson - prototype implementation".as_ptr(),
    c"Mixed strategy. First the homotopy solver is tried and then as fallback the hybrid solver.".as_ptr(),
    c"Damped Newton solver if failing case fixed-point and Newton homotopies are tried.".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static OMC_LOG_STREAM_NAME: CStrTable<57> = CStrTable([
    c"LOG_UNKNOWN".as_ptr(),
    c"LOG_STDOUT".as_ptr(),
    c"LOG_ASSERT".as_ptr(),
    c"LOG_DASSL".as_ptr(),
    c"LOG_DASSL_STATES".as_ptr(),
    c"LOG_DEBUG".as_ptr(),
    c"LOG_DELAY".as_ptr(),
    c"LOG_DIVISION".as_ptr(),
    c"LOG_DSS".as_ptr(),
    c"LOG_DSS_JAC".as_ptr(),
    c"LOG_DT".as_ptr(),
    c"LOG_DT_CONS".as_ptr(),
    c"LOG_EVENTS".as_ptr(),
    c"LOG_EVENTS_V".as_ptr(),
    c"LOG_GBODE".as_ptr(),
    c"LOG_GBODE_V".as_ptr(),
    c"LOG_GBODE_NLS".as_ptr(),
    c"LOG_GBODE_NLS_V".as_ptr(),
    c"LOG_GBODE_STATES".as_ptr(),
    c"LOG_INIT".as_ptr(),
    c"LOG_INIT_HOMOTOPY".as_ptr(),
    c"LOG_INIT_V".as_ptr(),
    c"LOG_IPOPT".as_ptr(),
    c"LOG_IPOPT_FULL".as_ptr(),
    c"LOG_IPOPT_JAC".as_ptr(),
    c"LOG_IPOPT_HESSE".as_ptr(),
    c"LOG_IPOPT_ERROR".as_ptr(),
    c"LOG_JAC".as_ptr(),
    c"LOG_LS".as_ptr(),
    c"LOG_LS_V".as_ptr(),
    c"LOG_MIXED".as_ptr(),
    c"LOG_MOO".as_ptr(),
    c"LOG_NLS".as_ptr(),
    c"LOG_NLS_V".as_ptr(),
    c"LOG_NLS_HOMOTOPY".as_ptr(),
    c"LOG_NLS_JAC".as_ptr(),
    c"LOG_NLS_JAC_TEST".as_ptr(),
    c"LOG_NLS_JAC_SUMS".as_ptr(),
    c"LOG_NLS_NEWTON_DIAGNOSTICS".as_ptr(),
    c"LOG_NLS_DERIVATIVE_TEST".as_ptr(),
    c"LOG_NLS_SVD".as_ptr(),
    c"LOG_NLS_SVD_V".as_ptr(),
    c"LOG_NLS_RES".as_ptr(),
    c"LOG_NLS_EXTRAPOLATE".as_ptr(),
    c"LOG_RES_INIT".as_ptr(),
    c"LOG_RT".as_ptr(),
    c"LOG_SIMULATION".as_ptr(),
    c"LOG_SOLVER".as_ptr(),
    c"LOG_SOLVER_V".as_ptr(),
    c"LOG_SOLVER_CONTEXT".as_ptr(),
    c"LOG_SOTI".as_ptr(),
    c"LOG_SPATIALDISTR".as_ptr(),
    c"LOG_STATS".as_ptr(),
    c"LOG_STATS_V".as_ptr(),
    c"LOG_SUCCESS".as_ptr(),
    c"LOG_SYNCHRONOUS".as_ptr(),
    c"LOG_ZEROCROSSINGS".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static OMC_LOG_STREAM_DESC: CStrTable<57> = CStrTable([
    c"unknown".as_ptr(),
    c"this stream is always active, can be disabled with -lv=-LOG_STDOUT".as_ptr(),
    c"this stream is always active, can be disabled with -lv=-LOG_ASSERT".as_ptr(),
    c"additional information about dassl solver".as_ptr(),
    c"outputs the states at every dassl call".as_ptr(),
    c"additional debug information".as_ptr(),
    c"debug information for delay operator".as_ptr(),
    c"Log division by zero".as_ptr(),
    c"outputs information about dynamic state selection".as_ptr(),
    c"outputs jacobian of the dynamic state selection".as_ptr(),
    c"additional information about dynamic tearing".as_ptr(),
    c"additional information about dynamic tearing (local and global constraints)".as_ptr(),
    c"additional information during event iteration".as_ptr(),
    c"verbose logging of event system".as_ptr(),
    c"information about GBODE solver".as_ptr(),
    c"verbose information about GBODE solver".as_ptr(),
    c"log non-linear solver process of GBODE solver".as_ptr(),
    c"verbose log non-linear solver process of GBODE solver".as_ptr(),
    c"output states at every GBODE call".as_ptr(),
    c"additional information during initialization".as_ptr(),
    c"log homotopy initialization".as_ptr(),
    c"verbose information during initialization".as_ptr(),
    c"information from Ipopt".as_ptr(),
    c"more information from Ipopt".as_ptr(),
    c"check jacobian matrix with Ipopt".as_ptr(),
    c"check hessian matrix with Ipopt".as_ptr(),
    c"print max error in the optimization".as_ptr(),
    c"outputs the jacobian matrix used by ODE solvers".as_ptr(),
    c"logging for linear systems".as_ptr(),
    c"verbose logging of linear systems".as_ptr(),
    c"logging for mixed systems".as_ptr(),
    c"logging for dynamic optimization library MOO".as_ptr(),
    c"logging for nonlinear systems".as_ptr(),
    c"verbose logging of nonlinear systems".as_ptr(),
    c"logging of homotopy solver for nonlinear systems".as_ptr(),
    c"outputs the jacobian of nonlinear systems".as_ptr(),
    c"tests the analytical jacobian of nonlinear systems".as_ptr(),
    c"tests the absolute sums of rows and cols in Kinsol Jacobian".as_ptr(),
    c"newton diagnostics (see: https://doi.org/10.1016/j.amc.2021.125991)".as_ptr(),
    c"test derivatives in KINSOL nonlinear systems".as_ptr(),
    c"perform a SVD analysis in KINSOL nonlinear systems".as_ptr(),
    c"perform a SVD analysis in KINSOL nonlinear systems (verbose)".as_ptr(),
    c"outputs every evaluation of the residual function".as_ptr(),
    c"outputs debug information about extrapolate process".as_ptr(),
    c"outputs residuals of the initialization".as_ptr(),
    c"additional information regarding real-time processes".as_ptr(),
    c"additional information about simulation process".as_ptr(),
    c"additional information about solver process".as_ptr(),
    c"verbose information about the integration process".as_ptr(),
    c"context information during the solver process".as_ptr(),
    c"final solution of the initialization".as_ptr(),
    c"logging of internal operations for spatialDistribution".as_ptr(),
    c"additional statistics about timer/events/solver".as_ptr(),
    c"additional statistics for OMC_LOG_STATS".as_ptr(),
    c"this stream is always active, unless deactivated with -lv=-LOG_SUCCESS".as_ptr(),
    c"log clocks and sub-clocks for synchronous features".as_ptr(),
    c"additional information about the zerocrossings".as_ptr(),
]);

#[unsafe(no_mangle)]
pub static firstOMCErrorStream: core::ffi::c_int = 1;

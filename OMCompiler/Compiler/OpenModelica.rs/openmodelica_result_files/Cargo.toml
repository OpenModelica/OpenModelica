# Result files without the compiler: the CSV/PLT readers, the suffix dispatch
# over them and the MATLAB v4 reader, and the diffSimulationResults tube
# comparison. Shared by `openmodelica_script_util` and the OMPlot web module.
[package]
name = "openmodelica_result_files"
version = "0.1.0"
edition = "2024"

[dependencies]
openmodelica_wasi.workspace = true
openmodelica_mat_reader.workspace = true

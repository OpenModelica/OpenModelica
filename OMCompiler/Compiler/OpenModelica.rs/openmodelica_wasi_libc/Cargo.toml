[package]
name = "openmodelica_wasi_libc"
version = "0.1.0"
edition = "2024"
# Not a native library, but the mechanism that hands this crate's OUT_DIR to a
# dependent's build script: openmodelica_wasm_jit reads the artifacts from there.
links = "omc_wasi_blobs"

# Builds (in build.rs) the external-"C" wasm artifacts a host-free FMI wasm FMU
# links in. The work is all in build.rs.
[dependencies]

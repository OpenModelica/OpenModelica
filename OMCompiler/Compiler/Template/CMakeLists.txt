

set(OMC_EXE ${PROJECT_SOURCE_DIR}/../build/bin/omc)

include(template_targets.cmake)

# a temporary target in the meantime.
# add_custom_target(
#     template_temp ALL
#     DEPENDS ${TPL_OUTPUT_MO_FILES}
# )


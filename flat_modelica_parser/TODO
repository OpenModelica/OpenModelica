Array subscripts does not work yet. (How should they work?)

Initial values of variables does not work.

An AST is built but not really used yet.

The grammar could probably be reduced as is currently parses modelica
as well as the subset flattened modelica.

A lot of testing, of course.
